{
  "name": "html-templates",
  "types": "src/index.ts",
  "main": "src/index.ts",
  "scripts": {
    "includecheck": "./check_includes.sh",
    "format:check": "pnpm -w prettier:glob --check ./libs/html-templates/src",
    "lint": "pnpm -w lintconfig:glob ./libs/html-templates/src",
    "prettier": "pnpm -w prettier:glob --write ./libs/html-templates/src",
    "check": "pnpm -w check:glob ./libs/html-templates/src",
    "typecheck": "tsc --noEmit",
    "fullcheck": "pnpm -w fullcheck --filter=html-templates"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^18.19.31",
    "typescript": "^5.4.5"
  }
}
