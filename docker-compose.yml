version: "3"
services:
  back:
    build: "./back"
    env_file:
      - .env

  front:
    build: "."
    env_file:
      - .env

  nginx:
    restart: unless-stopped
    depends_on:
      - front
      - back
    build:
      context: ./nginx
    ports:
      - "${EXPOSED_PORT:-80}:80"
